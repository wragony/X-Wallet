import 'package:flutter/material.dart';

import '../../../common/base/base_vm.dart';

class WalletSetupVM extends BaseViewModel {
  @override
  void onInit(BuildContext? context) {
    super.onInit(context);
  }

  @override
  void onDispose() {
    super.onDispose();
  }
}
